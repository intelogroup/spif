# Deps for provenance_comparison_bench.py only — not spif's normal deps.
# BENCHMARKS.md's recorded versions ("c2pa-python 0.37.5", "securesystemslib 1.4.0")
# don't exist on PyPI (max c2pa-python is 0.9.0, max securesystemslib is 1.3.1) —
# pinned to latest available instead.
c2pa-python==0.9.0
securesystemslib==1.3.1
cryptography>=41.0
