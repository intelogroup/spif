=== Test 3: Speed percentiles + cost breakdown (REPS=10000) ===
Platform: arm64 / arm / Darwin
Load avg (1/5/15m): 2.89 / 2.66 / 2.30

Decode latency, GC enabled (µs) — realistic in-process numbers:
  minimal  p50=4.96  p90=5.04  p99=7.08
  full     p50=47.83  p90=50.62  p99=166.79

Decode latency, GC disabled (µs) — steady-state, tail from GC sweeps removed:
  full     p50=48.17  p90=50.75  p99=127.33

  minimal p50 <10µs (GC on):       PASS
  full p99 <100µs (GC on, worst case): FAIL
  full p99 <100µs (GC off, steady-state): FAIL

Cost breakdown (full doc, signed, µs/op, mean of 2000 reps):
  full decode (signed, includes everything): 67.47µs
  full decode (unsigned, no ed25519 verify):  69.09µs
  ed25519 verify (isolated, signed-unsigned): 0.00µs
  verify_signature() end-to-end:               500.34µs
  SHA-256 over document bytes (proxy):         6.31µs
  CBOR parse, single node (proxy):             2.98µs
  DAG validation, 20-step trace:                13.60µs

NOTE: this run covers ONE architecture only (arm64). Re-run on x86 for the second data point required by the spec — do not claim cross-arch verification from this alone.
NOTE: Rust 450k/sec throughput is a SEPARATE claim (bench_speed.rs / spif-rust bench_speed bin) — not reproduced by this Python script.

TEST 3 (Python leg) OVERALL: FAIL (worst-case GC-on p99 will still occasionally exceed 100µs in-process — publish the GC-off steady-state number, and note the caveat) — x86 leg and Rust throughput leg still outstanding
